# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Structure

- `frontend/` — all frontend code
- `backend/` — all backend code
- All other files / general files like Dockerfile or scripts to run this project should be in the root folder of this project

Each directory has its own `CLAUDE.md` with stack-specific guidance (to be filled in as the project develops).
